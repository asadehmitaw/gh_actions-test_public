name: Defaults-Job-Level
on: push
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - run: pwd
  job2:
    defaults:
      run:
        working-directory: .github
        shell: shell
    runs-on: ubuntu-latest
    steps:
      - run: pwd