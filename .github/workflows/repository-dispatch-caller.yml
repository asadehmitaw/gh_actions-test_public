name: Repository-Dispatch-Caller
on: [push]
jobs:
  job1:
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - run: |
          curl -X POST \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: $GH_TOKEN" \
            https://api.github.com/repos/asadehmitaw/gh_actions-test_public/dispatches \
            -d '{"event_type":"event1","client_payload":{"env":"staging"}}'
          